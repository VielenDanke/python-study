import offset_appending

num_of_states = len(offset_appending.states_of_america)

print(offset_appending.states_of_america[num_of_states - 1])

first_list = ["a", "b", "c", "d"]
second_list = ["e", "f", "g"]

nested_list = [first_list, second_list]

print(nested_list)
